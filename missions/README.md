# Mission Packs

Mission packs are authored in YAML/JSON and validated by the Mission CLI.

## Structure
- `mission.yaml`: required mission definition
- `cards.yaml`: optional card grouping helper
- `rubric.yaml`: scoring weights
- `seed-levels/starter-level.json`: deterministic starter level for testing
- `assets/`: pixel art and mission-specific media
